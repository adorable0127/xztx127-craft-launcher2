<Window x:Class="XCL2.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource Str_Main_WindowTitlePrefix}" Height="640" Width="1000"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/app.ico"
        Background="{DynamicResource SideBrush}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧导航栏 -->
        <Border Grid.Column="0" Background="{DynamicResource SideBrush}" BorderBrush="{DynamicResource DividerBrush}" BorderThickness="0,0,1,0">
            <DockPanel Margin="12">
                <StackPanel DockPanel.Dock="Top">
                    <TextBlock Text="XCL2" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="6,10,0,20"/>
                    <Button Style="{StaticResource SideNavButton}" Click="NavHome_Click">
                        <TextBlock><Run Text="🏠 "/><Run Text="{DynamicResource Str_Nav_Home}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavVersions_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="📁 "/><Run Text="{DynamicResource Str_Nav_Versions}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavDownload_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="⬇️ "/><Run Text="{DynamicResource Str_Nav_Download}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavMultiplayer_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="🌐 "/><Run Text="{DynamicResource Str_Nav_Multiplayer}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavModManager_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="🧩 "/><Run Text="{DynamicResource Str_Nav_ModManager}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavServerManager_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="🖥 "/><Run Text="{DynamicResource Str_Nav_ServerManager}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavAccounts_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="👤 "/><Run Text="{DynamicResource Str_Nav_Accounts}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavSettings_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="⚙ "/><Run Text="{DynamicResource Str_Nav_Settings}"/></TextBlock>
                    </Button>
                    <Button Style="{StaticResource SideNavButton}" Click="NavLogs_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="📜 "/><Run Text="{DynamicResource Str_Nav_Logs}"/></TextBlock>
                    </Button>
                    <!-- 实验性功能导航按钮：仅当启动器界面语言是简体中文时显示（见
                         LocalizationService.ExperimentalFeaturesLanguageGate 的注释——这批功能
                         还没有多语言界面，贸然对其它语言用户展示未翻译的中文体验更差）。
                         x:Name 供 MainWindow.xaml.cs 在语言切换时动态控制 Visibility。 -->
                    <Button x:Name="NavExperimentalButton" Style="{StaticResource SideNavButton}" Click="NavExperimental_Click" Margin="0,4,0,0">
                        <TextBlock><Run Text="🧪 "/><Run Text="{DynamicResource Str_Nav_Experimental}"/></TextBlock>
                    </Button>
                </StackPanel>
                <StackPanel DockPanel.Dock="Bottom">
                    <TextBlock x:Name="CurrentAccountText" Text="未选择账户" FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="6,0,0,6" TextWrapping="Wrap"/>
                    <TextBlock x:Name="CurrentVersionText" Text="未选择版本" FontSize="12"
                               Foreground="{DynamicResource TextSecondaryBrush}" Margin="6,0,0,10" TextWrapping="Wrap"/>

                    <!-- 游戏进程控制按钮组：一键关闭 / 关闭所选 / 关闭未响应 -->
                    <UniformGrid Columns="3" Margin="0,0,0,8">
                        <Button Content="一键关闭" FontSize="10" Padding="2,6" Margin="0,0,2,0"
                                Style="{StaticResource SideNavButton}" Click="CloseAllGames_Click"
                                ToolTip="一键关闭游戏：结束所有正在运行的游戏进程"/>
                        <Button Content="关闭所选" FontSize="10" Padding="2,6" Margin="2,0,2,0"
                                Style="{StaticResource SideNavButton}" Click="CloseSelectedGame_Click"
                                ToolTip="关闭所选的游戏：从列表中选择一个正在运行的游戏并关闭"/>
                        <Button Content="关闭未响应" FontSize="10" Padding="2,6" Margin="2,0,0,0"
                                Style="{StaticResource SideNavButton}" Click="CloseUnresponsiveGame_Click"
                                ToolTip="关闭未响应的游戏：需要先手动勾选确认无响应，再强制结束，避免误杀"/>
                    </UniformGrid>

                    <Button x:Name="LaunchGameBtn" Content="启动游戏" Style="{StaticResource PrimaryButton}" FontSize="16" Padding="0,12"
                            Click="Launch_Click"/>
                </StackPanel>
                <Grid/>
            </DockPanel>
        </Border>

        <!-- 右侧内容区 -->
        <Border Grid.Column="1" Margin="16">
            <ContentControl x:Name="MainContent" RenderTransformOrigin="0.5,0.5">
                <ContentControl.RenderTransform>
                    <TranslateTransform x:Name="MainContentTransform" Y="0"/>
                </ContentControl.RenderTransform>
            </ContentControl>
        </Border>

        <!-- ===== 进程内弹窗宿主层（OverlayHost） =====
             说明见 Services/OverlayDialogService.cs 顶部的整体设计注释。这里只放"容器"：
             Grid.ColumnSpan="2" 让遮罩盖住整个主窗口（包括左侧导航栏），不是只盖内容区，
             跟独立 Window 弹窗"挡住整个程序窗口"的观感一致。默认 Collapsed，没有弹窗时
             完全不占用视觉/命中测试（Visibility=Collapsed 的元素不会拦截鼠标事件）。 -->
        <Grid x:Name="OverlayRoot" Grid.Column="0" Grid.ColumnSpan="2" Visibility="Collapsed" Focusable="False">
            <!-- 遮罩层：半透明黑，点击可选是否允许"点遮罩关闭"（由 OverlayDialogService 按每次
                 弹窗调用时的参数决定，见 OverlayOptions.DismissOnBackgroundClick）。 -->
            <Border x:Name="OverlayScrim" Background="#99000000" MouseLeftButtonUp="OverlayScrim_MouseLeftButtonUp"/>

            <!-- 弹窗内容容器：真正的弹窗 UserControl 通过 OverlayDialogService 塞进
                 OverlayContentHost.Content。外层 Border 提供跟原生 Window 弹窗一致的
                 卡片观感（圆角+PanelBrush 背景+阴影），弹窗自己的 XAML 不需要重复写这些。 -->
            <Border x:Name="OverlayCard" Background="{DynamicResource PanelBrush}" CornerRadius="8"
                    HorizontalAlignment="Center" VerticalAlignment="Center"
                    BorderBrush="{DynamicResource DividerBrush}" BorderThickness="1">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Opacity="0.35" BlurRadius="24" ShadowDepth="4"/>
                </Border.Effect>
                <Border.RenderTransform>
                    <ScaleTransform x:Name="OverlayCardScale" CenterX="0.5" CenterY="0.5"/>
                </Border.RenderTransform>
                <Border.RenderTransformOrigin>0.5,0.5</Border.RenderTransformOrigin>
                <ContentControl x:Name="OverlayContentHost" Focusable="False"/>
            </Border>
        </Grid>
    </Grid>
</Window>
